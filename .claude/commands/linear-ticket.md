Please analyze and work on the Linear ticket: $ARGUMENTS.

Follow these steps:

1. Use Linear MCP to get the ticket details and understand the problem
2. Search the codebase for relevant files and understand the current implementation
3. Implement the necessary changes to address the ticket requirements
4. Write and run tests to verify the implementation works correctly
5. Ensure code passes linting and type checking
6. Create a descriptive commit message that references the Linear ticket
7. Push changes and create a PR if requested

Use the Linear MCP tools to:
- Get ticket details with `mcp__linear__get_issue`
- List related tickets with `mcp__linear__list_issues`
- Update ticket status with `mcp__linear__update_issue`
- Add comments with `mcp__linear__create_comment`

Remember to keep the ticket updated with progress and link any PRs created.